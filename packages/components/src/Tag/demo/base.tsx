/**
 * description: 基本标签的用法，可以通过添加 closable 变为可关闭标签。可关闭标签具有 onClose 事件。
 */

import { Tag } from '@tenon-ui/components';

export default () => {
  return (
    <div>
      <Tag>tag1</Tag>
    </div>
  );
};
