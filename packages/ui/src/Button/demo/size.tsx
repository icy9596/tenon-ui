/**
 * description: 按钮有大、中、小三种尺寸。
 */

import { Button } from 'c-ui';

export default () => {
  return (
    <div>
      <Button>普通按钮</Button>
      <Button type="primary">主要按钮</Button>
    </div>
  );
};
