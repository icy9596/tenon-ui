/**
 * description: 按钮有大、中、小三种尺寸。
 */

import { Copy, Search, User } from '@tenon-ui/icons';

export default () => {
  return (
    <div>
      <Copy />
      <User />
      <Search />
    </div>
  );
};
